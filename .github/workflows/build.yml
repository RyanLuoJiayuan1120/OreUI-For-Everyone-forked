name: Build

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Build
        run: make

      - uses: actions/upload-artifact@v7
        with:
          name: resourcePack
          path: build/*.zip